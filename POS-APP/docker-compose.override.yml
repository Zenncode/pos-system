services:
  app:
    command: sh -c "pnpm install --frozen-lockfile && pnpm run dev -- --host 0.0.0.0 --port 5173"
    environment:
      - NODE_ENV=development
    ports:
      - '5173:5173'
    volumes:
      - ./:/app
      - /app/node_modules
      - /app/build
      - /app/.react-router