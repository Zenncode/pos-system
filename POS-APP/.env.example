# POS-APP env
# Point at your live POS-API. Leave empty to use Vite proxy (/api → http://localhost:3001, see vite.config.ts).
# POS-API host dev runs on :3001 (PORT in POS-API/.env); full-docker profile uses :3000.
VITE_API_URL=
